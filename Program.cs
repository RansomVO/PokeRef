using System;

namespace PokeRef
{
    /// <summary>
    /// This is just a dummy so that the compiler doesn't think it has to rebuild every time.
    /// </summary>
    class Program
    {
        static void Main(string[] args)
        {
        }
    }
}
